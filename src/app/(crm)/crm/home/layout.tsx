export const metadata = {
  title: "Home Management | CRM",
  description: "Manage homepage sections including hero, about, contact, and more",
};

export default function HomeManagementLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
